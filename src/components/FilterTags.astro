---
import { PRICE_CATEGORIES, STATUS_CATEGORIES, SUBJECT_CATEGORIES } from '../lib/categories';

const SORT_OPTIONS = [
  { value: 'price-asc', label: 'מחיר נמוך לגבוה' },
  { value: 'price-desc', label: 'מחיר גבוה לנמוך' },
] as const;

interface Props {
  activePrice?: string;
  activeStatus?: string;
  activeSubject?: string;
  activeSort?: string;
}

const { activePrice = '', activeStatus = '', activeSubject = '', activeSort = '' } = Astro.props;
---

<div class="filter-bar" id="filter-bar">
  <button type="button" class="filter-toggle" id="filter-toggle" aria-expanded="false" aria-controls="filter-rows">
    <span>סינון ומיון</span>
    <span class="filter-toggle-count" id="filter-count" hidden></span>
  </button>

  <div class="filter-rows" id="filter-rows">
  <div class="filter-row">
    <span class="filter-label">נושא:</span>
    <div class="filter-tags-wrap">
      <button type="button" class="filter-scroll-btn filter-scroll-prev" aria-label="גלילה לאחור" data-for="subject"></button>
      <div class="filter-tags" data-filter-type="subject">
      {SUBJECT_CATEGORIES.filter((cat) => cat.slug !== '').map((cat) => (
        <button
          type="button"
          class={`filter-tag ${activeSubject === cat.slug ? 'active' : ''}`}
          data-value={cat.slug}
        >
          {cat.label}
        </button>
      ))}
      </div>
      <button type="button" class="filter-scroll-btn filter-scroll-next" aria-label="גלילה להבא" data-for="subject"></button>
    </div>
  </div>
  <div class="filter-row">
    <span class="filter-label">מחיר:</span>
    <div class="filter-tags-wrap">
      <button type="button" class="filter-scroll-btn filter-scroll-prev" aria-label="גלילה לאחור" data-for="price"></button>
      <div class="filter-tags" data-filter-type="price">
      {PRICE_CATEGORIES.filter((cat) => cat.slug !== '').map((cat) => (
        <button
          type="button"
          class={`filter-tag ${activePrice === cat.slug ? 'active' : ''}`}
          data-value={cat.slug}
        >
          {cat.label}
        </button>
      ))}
      </div>
      <button type="button" class="filter-scroll-btn filter-scroll-next" aria-label="גלילה להבא" data-for="price"></button>
    </div>
  </div>
  <div class="filter-row">
    <span class="filter-label">סטטוס:</span>
    <div class="filter-tags-wrap">
      <button type="button" class="filter-scroll-btn filter-scroll-prev" aria-label="גלילה לאחור" data-for="status"></button>
      <div class="filter-tags" data-filter-type="status">
      {STATUS_CATEGORIES.filter((cat) => cat.slug !== '').map((cat) => (
        <button
          type="button"
          class={`filter-tag ${activeStatus === cat.slug ? 'active' : ''}`}
          data-value={cat.slug}
        >
          {cat.label}
        </button>
      ))}
      </div>
      <button type="button" class="filter-scroll-btn filter-scroll-next" aria-label="גלילה להבא" data-for="status"></button>
    </div>
  </div>
  <div class="filter-row">
    <span class="filter-label">מיון:</span>
    <div class="filter-tags" data-filter-type="sort">
      {SORT_OPTIONS.map((opt) => (
        <button
          type="button"
          class={`filter-tag ${activeSort === opt.value ? 'active' : ''}`}
          data-value={opt.value}
        >
          {opt.label}
        </button>
      ))}
    </div>
  </div>
  </div>
</div>

<style>
  .filter-bar {
    position: sticky;
    top: var(--header-h);
    z-index: 90;
    max-width: var(--page);
    margin: 0 auto;
    padding: 1rem var(--gutter) 0.85rem;
    background: rgba(247, 245, 241, 0.94);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line-soft);
  }

  .filter-toggle {
    display: none;
    align-items: center;
    gap: 0.5rem;
    width: 100%;
    padding: 0.55rem 0;
    background: none;
    border: none;
    font-family: var(--font-body);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    color: var(--ink-soft);
    cursor: pointer;
  }

  .filter-toggle::after {
    content: '';
    width: 7px;
    height: 7px;
    border-inline-end: 1.5px solid currentColor;
    border-bottom: 1.5px solid currentColor;
    transform: rotate(45deg);
    margin-bottom: 4px;
    transition: transform 0.25s ease;
  }

  .filter-toggle[aria-expanded='true']::after {
    transform: rotate(-135deg);
    margin-bottom: -2px;
  }

  .filter-toggle-count {
    padding: 0.05rem 0.5rem;
    border-radius: 999px;
    background: var(--ink);
    color: var(--paper);
    font-size: 0.72rem;
    letter-spacing: 0.04em;
  }

  .filter-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .filter-row:last-child {
    margin-bottom: 0;
  }

  .filter-label {
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    color: var(--ink-mute);
    min-width: 3.5rem;
  }

  .filter-tags-wrap {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    min-width: 0;
    flex: 1;
    position: relative;
  }

  .filter-scroll-btn {
    flex-shrink: 0;
    width: 28px;
    height: 28px;
    border: 1px solid var(--line);
    border-radius: 50%;
    background: var(--paper-white);
    color: var(--ink-soft);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s, border-color 0.2s;
    font-size: 0.75rem;
    position: relative;
    z-index: 2;
    box-shadow: 0 0 0 2px var(--paper);
  }

  .filter-scroll-btn:hover {
    background: var(--paper-deep);
    border-color: var(--ink-mute);
  }

  .filter-scroll-btn::before {
    content: '';
    width: 6px;
    height: 6px;
    border-inline-start: 2px solid currentColor;
    border-bottom: 2px solid currentColor;
    transform: rotate(-45deg);
    margin-inline-start: 2px;
  }

  .filter-scroll-next::before {
    transform: rotate(135deg);
    margin-inline-start: -2px;
  }

  .filter-tags {
    display: flex;
    flex-wrap: nowrap;
    gap: 0.5rem;
    align-items: center;
    overflow-x: auto;
    overflow-y: hidden;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    padding-inline-end: 0.25rem;
    min-width: 0;
    flex: 1;
  }

  .filter-tags::-webkit-scrollbar {
    height: 4px;
  }

  .filter-tags::-webkit-scrollbar-button {
    width: 0;
    height: 0;
    display: none;
  }

  .filter-tags::-webkit-scrollbar-track {
    background: var(--paper-deep);
    border-radius: 2px;
  }

  .filter-tags::-webkit-scrollbar-thumb {
    background: var(--line);
    border-radius: 2px;
  }

  .filter-tag {
    display: inline-block;
    flex-shrink: 0;
    padding: 0.42rem 1rem;
    font-family: var(--font-body);
    font-size: 0.85rem;
    font-weight: 400;
    letter-spacing: 0.03em;
    border: 1px solid var(--line);
    border-radius: 2rem;
    background: var(--paper-white);
    color: var(--ink-soft);
    cursor: pointer;
    transition: all 0.2s;
  }

  .filter-tag:hover {
    border-color: var(--ink-mute);
    color: var(--ink);
  }

  .filter-tag.active {
    border-color: var(--ink);
    background: var(--ink);
    color: var(--paper);
  }

  @media (max-width: 768px) {
    .filter-bar {
      padding: 0.35rem var(--gutter) 0.5rem;
    }

    .filter-toggle {
      display: flex;
    }

    /* Keeps the sticky bar to one line on phones until filters are needed */
    .filter-rows {
      display: none;
      padding-top: 0.6rem;
    }

    .filter-rows.is-open {
      display: block;
    }

    .filter-tag {
      font-size: 0.8rem;
      padding: 0.35rem 0.8rem;
    }
  }
</style>

<script>
  function getParams() {
    const url = new URL(window.location.href);
    return {
      price: url.searchParams.get('price') || '',
      status: url.searchParams.get('status') || '',
      subject: url.searchParams.get('subject') || '',
      sort: url.searchParams.get('sort') ?? '',
    };
  }

  function applyFilters(price: string, status: string, subject: string) {
    const items = document.querySelectorAll('.grid-item');
    items.forEach((el) => {
      const priceCat = (el as HTMLElement).dataset.priceCat || '';
      const priceNum = parseInt((el as HTMLElement).dataset.price || '', 10);
      const isDonation = (el as HTMLElement).dataset.donation === 'true';
      const sold = (el as HTMLElement).dataset.sold === 'true';
      const subjects = ((el as HTMLElement).dataset.subjects || '').split(',').filter(Boolean);

      let matchPrice = true;
      if (price === '250') {
        matchPrice = isDonation ? false : !isNaN(priceNum) && priceNum <= 250;
      } else if (price === '250-400') {
        matchPrice = isDonation ? false : !isNaN(priceNum) && priceNum > 250 && priceNum <= 400;
      } else if (price === '400-800') {
        matchPrice = isDonation ? false : !isNaN(priceNum) && priceNum > 400 && priceNum <= 800;
      } else if (price === '800+') {
        matchPrice = isDonation ? false : !isNaN(priceNum) && priceNum > 800;
      }

      const matchStatus =
        !status ||
        (status === 'available' && !sold) ||
        (status === 'sold' && sold);

      const matchSubject = !subject || subjects.includes(subject);

      (el as HTMLElement).style.display = matchPrice && matchStatus && matchSubject ? '' : 'none';
    });
  }

  function updateUrl(price: string, status: string, subject: string, sort: string) {
    const url = new URL(window.location.href);
    if (price) url.searchParams.set('price', price);
    else url.searchParams.delete('price');
    if (status) url.searchParams.set('status', status);
    else url.searchParams.delete('status');
    if (subject) url.searchParams.set('subject', subject);
    else url.searchParams.delete('subject');
    if (sort) url.searchParams.set('sort', sort);
    else url.searchParams.delete('sort');
    window.history.replaceState({}, '', url.toString());
  }

  function reorderGrid(sort: string) {
    const grid = document.querySelector('#artwork-grid .grid');
    if (!grid) return;
    if (!sort) return;
    const order = sort === 'price-desc' ? 'price-desc' : 'price-asc';
    const items = Array.from(grid.querySelectorAll('.grid-item'));
    const sorted = items.slice().sort((a, b) => {
      const elA = a as HTMLElement;
      const elB = b as HTMLElement;
      const parseNum = (el: HTMLElement) =>
        el.dataset.donation === 'true' ? 0 : (parseInt(el.dataset.price || '', 10) || -1);
      const na = parseNum(elA);
      const nb = parseNum(elB);
      return order === 'price-desc' ? nb - na : na - nb;
    });
    sorted.forEach((node) => grid.appendChild(node));
  }

  function shuffleGrid() {
    const grid = document.querySelector('#artwork-grid .grid');
    if (!grid) return;
    const items = Array.from(grid.querySelectorAll('.grid-item'));
    for (let i = items.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [items[i], items[j]] = [items[j], items[i]];
    }
    items.forEach((node) => grid.appendChild(node));
  }

  function setActiveButton(type: 'price' | 'status' | 'subject' | 'sort', value: string) {
    const container = document.querySelector(`[data-filter-type="${type}"]`);
    container?.querySelectorAll('.filter-tag').forEach((btn) => {
      btn.classList.toggle('active', (btn as HTMLElement).dataset.value === value);
    });
  }

  function updateCount() {
    const badge = document.getElementById('filter-count');
    if (!badge) return;
    const { price, status, subject, sort } = getParams();
    const active = [price, status, subject, sort].filter(Boolean).length;
    badge.textContent = String(active);
    badge.hidden = active === 0;
  }

  document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('filter-bar');
    const toggle = document.getElementById('filter-toggle');
    const rows = document.getElementById('filter-rows');
    const { price, status, subject, sort } = getParams();

    toggle?.addEventListener('click', () => {
      const open = rows?.classList.toggle('is-open') ?? false;
      toggle.setAttribute('aria-expanded', String(open));
    });

    applyFilters(price, status, subject);
    updateCount();
    setActiveButton('price', price);
    setActiveButton('status', status);
    setActiveButton('subject', subject);
    setActiveButton('sort', sort);

    if (!sort) shuffleGrid();

    bar?.querySelectorAll('.filter-scroll-btn').forEach((scrollBtn) => {
      scrollBtn.addEventListener('click', () => {
        const wrap = (scrollBtn as HTMLElement).closest('.filter-tags-wrap');
        const tagsEl = wrap?.querySelector('.filter-tags') as HTMLElement | null;
        if (!tagsEl) return;
        const isNext = (scrollBtn as HTMLElement).classList.contains('filter-scroll-next');
        const delta = 180;
        const rtl = document.documentElement.dir === 'rtl';
        const scrollAmount = isNext ? (rtl ? -delta : delta) : (rtl ? delta : -delta);
        tagsEl.scrollBy({ left: scrollAmount, behavior: 'smooth' });
      });
    });

    bar?.addEventListener('click', (e) => {
      const btn = (e.target as HTMLElement).closest('.filter-tag');
      if (!btn) return;
      setTimeout(updateCount, 0);

      const type = (btn.closest('[data-filter-type]') as HTMLElement)?.dataset.filterType;
      const value = (btn as HTMLElement).dataset.value || '';

      if (type === 'price') {
        const { price: currentPrice, status: s, subject: sub, sort: sortVal } = getParams();
        const newPrice = currentPrice === value ? '' : value;
        updateUrl(newPrice, s, sub, sortVal);
        setActiveButton('price', newPrice);
        applyFilters(newPrice, s, sub);
      } else if (type === 'status') {
        const { price: p, status: currentStatus, subject: sub, sort: sortVal } = getParams();
        const newStatus = currentStatus === value ? '' : value;
        updateUrl(p, newStatus, sub, sortVal);
        setActiveButton('status', newStatus);
        applyFilters(p, newStatus, sub);
      } else if (type === 'subject') {
        const { price: p, status: s, subject: currentSubject, sort: sortVal } = getParams();
        const newSubject = currentSubject === value ? '' : value;
        updateUrl(p, s, newSubject, sortVal);
        setActiveButton('subject', newSubject);
        applyFilters(p, s, newSubject);
      } else if (type === 'sort') {
        const { price: p, status: s, subject: sub, sort: currentSort } = getParams();
        const newSort = currentSort === value ? '' : value;
        updateUrl(p, s, sub, newSort);
        setActiveButton('sort', newSort);
        if (newSort) reorderGrid(newSort);
        else shuffleGrid();
      }
    });
  });
</script>
